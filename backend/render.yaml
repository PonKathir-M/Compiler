services:
  - type: web
    name: compiler-backend
    env: node
    rootDir: backend
    buildCommand: |
      apt-get update
      apt-get install -y openjdk-17-jdk
      npm install
    startCommand: node index.js